{
  "name": "my-local-market",
  "version": "1.0.0",
  "description": "Hyper-local commerce service for Jinro Mart Mokgam",
  "scripts": {
    "dev": "concurrently \"npm run db:up\" \"npm run dev:front\"",
    "dev:front": "npm run dev --prefix frontend -- -p 3001",
    "dev:python": "docker compose up -d db python-backend",
    "db:up": "docker compose up -d db python-backend",
    "db:down": "docker compose down",
    "transition:up": "docker compose --profile transition-next up -d --build db python-backend next-frontend",
    "legacy:up": "docker compose --profile legacy-java up -d --build db python-backend java-frontend",
    "mvp:up": "npm run transition:up",
    "mvp:down": "docker compose down"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}

# Java Frontend (Spring Boot + Thymeleaf)

## Run locally

```bash
mvn spring-boot:run
```

If backend is not local default, set:

```bash
export MARKET_API_BASE_URL='http://localhost:8000/api/v1'
```
